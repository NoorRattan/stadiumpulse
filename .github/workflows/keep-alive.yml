name: Keep Render Warm

on:
  schedule:
    - cron: "0 8 */3 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl --fail --show-error --silent "${{ secrets.RENDER_HEALTH_URL }}"
